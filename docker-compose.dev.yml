services:
  db:
    container_name: TIWTBD
    image: mysql:8.0.21
    platform: linux/x86_64
    restart: always
    ports:
      - 3316:3306
    environment:
      MYSQL_ROOT_PASSWORD: password

networks:
  default:
    name: TIWTBD